# INDEX — ルーティング表

**AIが常時読むのはこのファイルだけである。** 該当した行のファイルだけを開く。
リポジトリ全体を読み込まない。

状態が `未作成` の行に当たったら、**そのことをユーザに伝え、本リポジトリを根拠に
使わない**。無い内容を推測で埋めない。

---

## 0. 実行手順

この順に実行する。飛ばさない。

1. **媒体を確認する** → 第1節。未対応ならここで止める
2. **モードを判定する** → 第2節
3. **デッキのジャンルを1つだけ選ぶ** → 第3節
4. **置く要素の数だけ要素プレイブックを読む** → 第4節（0本でもよい）
5. **制約に当たるものを読む** → 第5節（0本でもよい）
6. **生成の直前に決め打ち値と禁止事項を読む** → 第6節
7. **生成後に、読んだ全プレイブックの `done_when` を1つずつ確認する**
8. **見ないと判定できない項目を列挙してユーザに返す**（第8節）

## 1. 媒体

| 媒体 | 状態 |
|---|---|
| PowerPoint（`.pptx`） | v0.1 対応 |
| web / HTML、chart 単体、diagram 単体、文書 | **未対応**（v0.3 以降）。本リポジトリを根拠に使わない |

## 2. モード（必ず1つ）

| when | 読むもの | 状態 |
|---|---|---|
| 成果物がまだ存在しない | 何も読まず第3節へ | — |
| 既存の `.pptx` を直す・作り直す・レビューする | `playbooks/pptx-mode-refactor.md` を**先に**読み、そのあと第3節へ | 未作成 |

## 3. デッキのジャンル（必ず1つだけ）

判定は「**この資料のあと、聞き手に何をしてほしいか**」で行う。

| id | when | ファイル | 状態 |
|---|---|---|---|
| `pptx-deck-decision` | 聞き手に**決めて**ほしい（決裁・承認・予算・採用可否） | `playbooks/pptx-deck-decision.md` | 未作成 |
| `pptx-deck-explain` | 聞き手に**分かって**ほしい。決裁は求めない | `playbooks/pptx-deck-explain.md` | 未作成 |
| `pptx-deck-pitch` | 聞き手に**関心を持って**ほしい（次の接点を作る。社外・短時間） | `playbooks/pptx-deck-pitch.md` | 未作成 |

**2つ以上に当てはまるとき**: 最終スライドで相手に求める行動を1つだけ書き出し、
それに対応するものを選ぶ。2つ選んではならない。

**判定できないとき**: 推測せず、ユーザに
「この資料を見た相手に、決めてほしいのか／分かってほしいのか／興味を持ってほしいのか」
を聞く。

## 4. 要素（置く分だけ。0本以上）

| id | when | ファイル | 状態 |
|---|---|---|---|
| `pptx-element-table` | 表を置く | `playbooks/pptx-element-table.md` | 未作成 |
| `pptx-element-chart` | 定量データをグラフで示す | `playbooks/pptx-element-chart.md` | 未作成 |
| `pptx-element-diagram` | 構造・関係・流れを図で示す | `playbooks/pptx-element-diagram.md` | 未作成 |

## 5. 制約（該当する分だけ。0本以上）

| id | when | ファイル | 状態 |
|---|---|---|---|
| `pptx-constraint-template` | 指定の `.pptx` / `.potx` テンプレートがある | `playbooks/pptx-constraint-template.md` | 未作成 |

## 6. 生成の直前に必ず読む

| ファイル | 中身 | 状態 |
|---|---|---|
| `tokens/pptx.tokens.json` | 決め打ち値（色4枠・型スケール・グリッド・密度予算）。DTCG 形式 | v0.1 |
| `antipatterns/pptx.md` | 具体名で禁止された事項 | 未作成 |
| `glossary.md` | 語彙。用語に迷ったときのみ | v0.1 |

## 7. 衝突解決の順序

指示が衝突したら、この順で上が勝つ。迷ってはならない。

1. **MUST** — 外部の規範（可読性・アクセシビリティの下限）。逸脱は欠陥
2. **ユーザ／組織の明示指定** — ブランド、コーポレートテンプレート
3. **SHOULD** — 業界の広い合意
4. **HOUSE** — 本リポジトリの決め打ち

`HOUSE` が上書きされるのは失敗ではない。決めないと出力が学習データの最頻値に
落ちるため、既定値として置いてあるだけである。

## 8. 本リポジトリで判定できないこと

以下はテキストでは判定できない。**できたことにせず、ユーザに名指しで返す。**

- 美しさ・印象・好み
- ブランドらしさへの適合
- 写真・イラストの選定の適否
- 実際にレンダリングした結果の見た目（文字のはみ出し、図形の重なり）

## 9. ファイル生成の実装

本リポジトリは**仕様と完了条件のみ**を供給する。`.pptx` の生成・抽出は
`pptx-build` に渡す。python-pptx を直接書かない。
